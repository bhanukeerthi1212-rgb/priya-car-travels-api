{
  "name": "priya-car-travels-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch database.json --port 4000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
